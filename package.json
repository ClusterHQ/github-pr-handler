{
  "name": "github_pr_handler",
  "version": "0.0.1",
  "description": "Receive Github PRs and trigger the appropriate jobs on Jenkins",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test && eslint . && mocha && unset NODE_ENV"
  },
  "author": "ClusterHQ",
  "devDependencies": {
    "chai": "^3.4.0",
    "eslint": "^1.8.0",
    "eslint-config-defaults": "^7.1.1",
    "mocha": "^2.3.3"
  },
  "dependencies": {
    "body-parser": "^1.14.1",
    "command-line-args": "^2.0.2",
    "console-stamp": "^0.2.0",
    "express": "^4.13.3",
    "request-promise": "^1.0.2"
  }
}
